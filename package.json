{
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4"
  },
  "scripts": {
    "build": "tsc --build ./tsconfig.json",
    "test": "jest --watch"
  }
}
